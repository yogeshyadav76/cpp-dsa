#include <iostream>
#include <vector>
using namespace std ;
int main (){
    int size;
    cout << "Enter the size of vector " << endl ;
    cin >> size ;
    vector<int> vec ;
    // we have direct command to reverse array reverse(v.begin(), v.end());
    for(int i=0 ; i<size;i++){
        vec 
    }
    return 0;
}